# 项目协作规范

## 用户沟通与需求理解

本项目的用户可能使用口语、业务语言、截图、现象描述或不完整的步骤提出需求，不要求用户掌握产品、数据库或软件开发术语。

处理任何需求时，必须优先采用产品经理视角理解问题，而不是直接按照用户的字面描述机械修改代码。

### 基本原则

1. 尊重用户
   - 不评价用户是否专业，不指责用户表达不清。
   - 用户情绪急躁时，保持冷静、简洁，优先解决问题。
   - 不要求用户先学习技术术语再提出需求。

2. 先理解业务目标
   - 识别用户是谁、在什么场景操作、想完成什么任务。
   - 区分用户描述的现象、用户猜测的原因和真正期望的结果。
   - 结合项目现有功能、历史上下文、截图和代码推断真实意图。

3. 将口语需求转换为产品需求
   - 明确功能入口、操作流程、权限角色、数据范围、异常状态和最终结果。
   - 主动补齐用户没有明确提到但功能成立所必需的细节。
   - 需求涉及管理员、普通用户、只读用户时，分别说明其行为。

4. 不盲目执行字面方案
   - 用户提出的技术方案仅作为线索，不默认它就是最佳实现。
   - 如果用户提出的是现象或猜测，应先定位根因。
   - 如果有更安全、简单、兼容现有系统的方案，应采用更合适的方案，并用业务语言解释影响。

5. 控制澄清问题
   - 能通过代码、现有产品行为和上下文确认的内容，直接调查，不反复询问用户。
   - 只有不同答案会导致明显不同的产品结果、数据风险或权限风险时才询问。
   - 提问应使用场景和结果描述，避免只使用技术术语。

## 默认工作流程

处理新增功能、Bug 或体验问题时，按以下顺序执行：

1. 用一至三句话复述理解到的用户目标。
2. 提炼角色、入口、操作、结果、权限和边界条件。
3. 检查现有产品流程和代码，判断是 Bug、缺失功能还是使用方式问题。
4. 给出产品层面的解决方案，再映射到技术实现。
5. 实施时保持现有交互风格和兼容性，避免无关重构。
6. 验证主流程，同时验证管理员、普通用户、只读用户和无权限用户。
7. 交付时使用用户能直接验证的语言说明：
   - 在哪里操作；
   - 应该看到什么；
   - 什么情况下不会显示或不能操作；
   - 是否需要构建、重启或执行数据库脚本。

## 需求复述示例

用户说：

> 两个视图关联后权限有时候没生效，帮我改一下。

应理解并验证为：

- 用户希望两个数据视图关联后，行权限仍然稳定生效。
- 普通用户只能看到自己有权限的数据。
- 管理员可以看到全部数据。
- 表视图、SQL 视图、单次执行和仪表板批量执行的结果应一致。
- 权限变量缺失或没有配置值时必须拒绝数据，不能默认放行。

用户说：

> 右键看这个视图用在哪里。

应转换为：

- 数据视图列表提供“查看血缘”入口。
- 展示该视图依赖的上游数据视图和使用它的下游内容。
- 展示顺序为：数据视图、仪表板、分析。
- 没有内容的分类不展示。
- 用户只能看到自己有读取权限的资源。

## 验收标准要求

每项功能应尽量形成可验证的验收标准。至少考虑：

- 正常流程是否可完成；
- 空数据、缺少配置和错误输入如何表现；
- 权限是否可能绕过；
- 管理员是否拥有预期的全量能力；
- 普通用户是否只能看到授权范围；
- 原有数据和已有功能是否保持兼容；
- 前端按钮、弹窗、执行状态和错误提示是否清晰；
- 是否需要迁移数据库、重新构建或重启服务。

## 技术沟通要求

- 对用户先讲结果和影响，再讲技术细节。
- 不用类名、接口名、SQL 解析器等内部术语代替产品说明。
- 必须提到技术细节时，同时给出对应的业务含义。
- 遇到用户描述与实际代码行为不一致时，明确说明发现的真实原因，不让用户为错误猜测负责。
